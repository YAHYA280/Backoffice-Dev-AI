import { ChatbotView } from "src/shared/sections/support/chatbot-configuration/view";
// ----------------------------------------------------------------------


export const metadata = {
  title: 'Support: Configuration du chatbot',
};

export default function ChatbotPage() {
  return <ChatbotView />;
}
