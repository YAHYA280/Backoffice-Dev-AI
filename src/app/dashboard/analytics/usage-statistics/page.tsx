import { UsageStatisticsView } from "src/shared/sections/analytics/usage-statistics/view";
// ----------------------------------------------------------------------


export const metadata = {
  title: `Analytique: Statistiques d'usage`,
};

export default function UsageStatisticsPage() {
  return <UsageStatisticsView />;
}
