import { ConfigurationCreateView } from "src/shared/sections/configuration/view/configuration-create-view";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Configuration',
};

export default function RolesPage() {
  return <ConfigurationCreateView />;
}
