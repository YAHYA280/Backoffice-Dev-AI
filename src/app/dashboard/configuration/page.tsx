import { ConfigurationView } from "src/shared/sections/configuration/view/configuration-view";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Configuration',
};

export default function RolesPage() {
  return <ConfigurationView />;
}
