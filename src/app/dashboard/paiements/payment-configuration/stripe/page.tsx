import StripeConfigurationView from 'src/shared/sections/paiements/payment-configuration/views/StripeConfigurationView';

export const metadata = {
  title: 'Configuration Stripe - Dashboard',
};

export default function StripeConfigPage() {
  return <StripeConfigurationView />;
}
