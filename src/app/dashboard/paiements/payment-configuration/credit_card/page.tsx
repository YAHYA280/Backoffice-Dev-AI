import CreditCardConfigurationView from 'src/shared/sections/paiements/payment-configuration/views/CreditCardConfigurationView';

export const metadata = {
  title: 'Configuration Carte Bancaire - Dashboard',
};

export default function CreditCardConfigPage() {
  return <CreditCardConfigurationView />;
}
