import StripeConfigView from 'src/shared/sections/paiements/payment-configuration/views/StripeConfigView';

export const metadata = {
  title: 'Configuration Stripe - Dashboard',
};

export default function StripeConfigPage() {
  return <StripeConfigView />;
}
