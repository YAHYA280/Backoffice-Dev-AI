import React from 'react';

import NotificationChannels from "src/shared/sections/profile/notifications/channels/page";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Canaux de réception des notifications',
};

export default function NotificationChannelsPage() {
  return <NotificationChannels />;
}